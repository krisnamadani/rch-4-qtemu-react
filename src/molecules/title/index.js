import React from "react";
import Heading from "../../atoms/heading";

export default function Title(props) {
    return (
        <div className="title mr-8">
            <Heading className="text-white font-semibold">QTemu</Heading>
        </div>
    );
}